# Generated by Tama Kit. Exact local HTTPS MCP App proxy; no catch-all route.
{{PROVIDER_HOST}} {
  tls /etc/tama-kit/tls/local.pem /etc/tama-kit/tls/local-key.pem
  reverse_proxy {{PROVIDER_UPSTREAM}} {
    header_up Host {host}
    header_up X-Forwarded-Proto {scheme}
    header_up X-Forwarded-Host {host}
  }
}

{{TAMA_HOST}} {
  tls /etc/tama-kit/tls/local.pem /etc/tama-kit/tls/local-key.pem
  reverse_proxy {{TAMA_UPSTREAM}} {
    header_up Host {host}
    header_up X-Forwarded-Proto {scheme}
    header_up X-Forwarded-Host {host}
  }
}
